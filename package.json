{
  "name": "gonogo-uplinks",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "prepare": "git config core.hooksPath .githooks || true"
  }
}
