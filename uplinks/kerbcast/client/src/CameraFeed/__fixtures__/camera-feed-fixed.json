{
  "_scene": {
    "widget": "camera-feed",
    "modes": ["default", "mobile-9x8", "landscape-18x5"],
    "config": { "flightId": 42 },
    "caption": "A fixed camera, which most are: no aim to give it, so the feed has the whole tile and the header carries the name and the signal delay",
    "paints": ["Nose Cam"]
  },
  "_stream": {
    "pinnedUt": 1000000,
    "emits": [{ "topic": "comms.delay", "payload": { "oneWaySeconds": 1.4 } }]
  }
}
