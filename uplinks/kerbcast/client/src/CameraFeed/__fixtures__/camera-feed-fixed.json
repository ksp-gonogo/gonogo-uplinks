{
  "_scene": {
    "widget": "camera-feed",
    "modes": ["default", "min", "mobile-9x8"],
    "config": { "flightId": 42 },
    "caption": "A fixed camera, which most are: no aim to give it, so the picture has the whole panel body and the feed's own header carries the name and the signal delay",
    "before": [{ "hover": "video" }],
    "paints": ["Nose Cam"]
  },
  "_stream": {
    "pinnedUt": 1000000,
    "emits": [{ "topic": "comms.delay", "payload": { "oneWaySeconds": 1.4 } }]
  }
}
