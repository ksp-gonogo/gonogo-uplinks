{
  "_meta": {
    "notes": "Two booted CPUs and no pinned tagname, which is the state the CPU picker exists for: with one CPU the widget targets it silently and the picker is not drawn at all. Then the link drops: the transport disconnects after the scene is staged, so every reading this widget draws is held rather than current.",
    "scenario": "two-cpus-idle-stopped-arriving"
  },
  "_scene": {
    "widget": "kos-script-trigger",
    "caption": "The same two CPUs once the link drops",
    "config": {
      "scriptPath": "0:/descent.ks"
    },
    "modes": ["default", "min"]
  },
  "_stream": {
    "pinnedUt": 1000000,
    "emits": [
      {
        "topic": "kos.processors",
        "payload": [
          {
            "coreId": 7,
            "tag": "lander",
            "hasBooted": true,
            "processorMode": "READY"
          },
          {
            "coreId": 8,
            "tag": "relay",
            "hasBooted": true,
            "processorMode": "READY"
          }
        ]
      }
    ],
    "stopsArriving": true
  }
}
