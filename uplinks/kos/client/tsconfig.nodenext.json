{
  "//": "The same sources under moduleResolution nodenext. The two modes disagree SILENTLY: `declare module \"./types\"` binds under bundler and does not under nodenext, so a declaration merge vanishes and every key it contributed goes with it. This client's topics.ts declares its Topics through exactly that mechanism, so it is the pattern most worth holding to both modes.",
  "extends": "@ksp-gonogo/sitrep-sdk/tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "module": "nodenext",
    "moduleResolution": "nodenext"
  },
  "include": ["src"]
}
