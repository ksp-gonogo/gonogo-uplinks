{
  "_meta": {
    "notes": "SCANsat reported absent. The state a player without the mod sees, and the one that must not read as 'no data yet': the widget names what installing SCANsat would give them."
  },
  "_scene": {
    "widget": "scanning",
    "caption": "SCANsat is not installed, so the widget says what it would show if it were",
    "modes": ["default"]
  },
  "_stream": {
    "pinnedUt": 1000000,
    "emits": [{ "topic": "scansat.available", "payload": false }]
  }
}
